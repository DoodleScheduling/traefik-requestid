displayName: Add request ID
type: middleware
import: github.com/DoodleScheduling/traefik-requestid
summary: 'Adds a unique request id header both up and downstrea'
testData:
  headerName: 'X-Request-Id'
